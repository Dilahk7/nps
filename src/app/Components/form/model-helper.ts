export class npsmodel {
  salary: number;
  percent: number;

  constructor(salary: number, percent: number) {
    this.salary = salary;
    this.percent = percent;
  }
}
export class SetVals {
  pran: number;
  finval: number;

  constructor(pran: number, finval: number) {
    this.pran = pran;
    this.finval = finval;
  }
}
